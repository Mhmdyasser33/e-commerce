/* import { useLocation } from "react-router-dom"

export default function Test() {
    const {search} = useLocation() ;  // return every thing after ? 
    const redirectUrl = new URLSearchParams(search).get('redirect') ; // search for value of key names redirect 
    const redirect = redirectUrl ? redirectUrl : "/" ; 
    console.log(location);
  return (
    <div>Test</div>
  )
}
 */